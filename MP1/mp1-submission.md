<table><tr><td> <em>Assignment: </em> IS601 - Mini Project 1 - Tracker App</td></tr>
<tr><td> <em>Student: </em> Akhilesh Ravi (ar2565)</td></tr>
<tr><td> <em>Generated: </em> 2/20/2023 7:41:47 PM</td></tr>
<tr><td> <em>Grading Link: </em> <a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IS601-004-S23/is601-mini-project-1-tracker-app/grade/ar2565" target="_blank">Grading</a></td></tr></table>
<table><tr><td> <em>Instructions: </em> <ol><li>Checkout dev branch and pull any pending changes&nbsp;</li><ol><li>&nbsp;git checkout dev</li><li>&nbsp;git pull origin dev</li></ol><li>Create a new branch for this assignment (see Desired Branch Name)</li><ol><li>git checkout -b MP1-Tracker</li></ol><li>Create a new folder called MP1 in your local repository</li><li>Create a new file called tracker.py</li><li>Copy/paste the content from this template:&nbsp;&nbsp;<a href="https://gist.github.com/MattToegel/380e6baa24f6c25b74bf2ce99ccba6da">https://gist.github.com/MattToegel/380e6baa24f6c25b74bf2ce99ccba6da</a></li><li>Add/commit/push the template file</li><ol><li>git add --all</li><li>git commit -m "adding template"</li><li>git push origin MP1-Tracker</li></ol><li>Create a pull request from MP1-Tracker to dev (keep it open, do not close it until you're done)</li><li>Solve the various todo items (also noted below in the deliverables) and fill in the evidence</li><ol><li>Periodically add/commit; recommended after each solved item or every few items</li></ol><li>Save and copy/download the markdown</li><li>Create a new file mp1-submission.md in the MP1 folder</li><li>Add the markdown content</li><li>add/commit/push all the pending files for this assignment (tracker.py and mp1-submission.md)</li><li>If everything looks good on the pull request complete the merge</li><li>Create a new pull request from dev to prod and merge it to update prod (not used yet but you want to keep this up to date)</li><li>checkout dev locally and pull the changes to be up to date</li><li>Navigate to the prod branch on github and find the mp1-submission.md file and get the link to the file to submit to canvas</li></ol></td></tr></table>
<table><tr><td> <em>Deliverable 1: </em> Add Task Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited add_task() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220061456-27622d66-946c-494a-ae7e-546379ea1513.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p> in the below code we are giving the values of name,description and<br>due date to a dictionary named task.  if all the values are<br>given as an input, this shows a message that the task is added<br>successfully or else it displays a failure message<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of add_task()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220065634-2849c63d-9d76-489c-820a-b26c624c2eb0.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>The program offers several commands, including add, update, view, list, overdue, delete, remaining,<br>done, quit, and help.  The user first selects the &quot;add&quot; command and<br>enters details for a task called &quot;sports&quot; with a description of &quot;playing badminton&quot;<br>and a due date of February 23rd, 2023 at 10:00:00. The program successfully<br>adds the task.  The user then attempts to add another task called<br>&quot;study time&quot; with a due date of February 24th, 2023 at 6:00:00.But, the<br>input for description is not given.Then the print statement of &quot;task not added<br>successfully&quot; is shown<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for add_task()</td></tr>
<tr><td> <em>Response:</em> <div>The add_task function takes three parameters: name, description, and due.&nbsp;</div><div><br></div><div>A try-except block is<br>used to catch any errors that may occur when trying to convert the<br>due parameter to a datetime object using a str_to_datetime function.</div><div>The task information is<br>stored in a dictionary called task, with keys for the task name, description,<br>due date, and last activity time.</div><div>The tasks list is updated with the task<br>dictionary using the append method.</div><div>If an error occurs while trying to convert the<br>due parameter to a datetime object, the except block is executed, and an<br>error message is printed to the console.</div><div>If any of the required data is<br>missing, the else block is executed, and a message is printed to the<br>console.</div><div>Finally, the save function is called.</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 2: </em> Process Update Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited process_update() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220065929-7117490a-fc79-49e2-97f7-fbc1665f3efd.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>This code gets the task by index, it checks whether the index is<br>out of bonds or not.If yes it prints a statement of the same.<br>Otherwise, the code takes the inputs of task name, description and due date.After<br>this, these patameters are passed to update_task()<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Briefly explain the solutions to the checklist items for process_update()</td></tr>
<tr><td> <em>Response:</em> <div>This code defines a function called process_update() which takes an index parameter as<br>input.</div><div><br></div><div>The function first tries to retrieve the task at the specified index from<br>a list called tasks. If the index is invalid, an error message is<br>printed and the function returns.</div><div><br></div><div>If the index is valid, the function prompts the<br>user to enter new values for the name, description, and due date of<br>the task. If the user enters a value, it is used as the<br>new value for that property. If the user enters an empty string or<br>whitespace, the existing value for that property is used.</div><div><br></div><div>The update_task() function is then<br>called with the updated values for the name, description, and due date of<br>the task.</div><div><br></div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 3: </em> Update Task Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited update_task() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220066293-b5dcea50-7ffe-4513-91f8-164e6a65a6b9.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>this code finds the task by index. If there is any index out<br>of bonds,it displays the error message. this code takes the values from the<br>previous function process_update() and updates the task if any inputs are provided if<br>inputs are not provided, it displays a print statement regarding the same.It also<br>updates the last activity to the current datetime<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of update_task()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220066518-526ea583-339e-4f3b-91d9-03de41df4d5f.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>The program prompts the user to select the task they want to update<br>by entering its number from the &quot;list&quot; command.  The user selects task<br>number 1, which is currently named &quot;sports&quot; and has a description of &quot;playing<br>badminton&quot; and a due date of February 23rd, 2023 at 10:00:00. The user<br>updates the task name to &quot;drawing&quot;, changes the description to &quot;paint a beautiful<br>scenery&quot;, and updates the due date to February 25th, 2023 at 07:30:00. <br>The program confirms that the task was updated successfully.Later, again the input from<br>user is given as update and no further inputs are given to update<br>the tasks.the code prints that the user did not make any modifications during<br>their second attempt at updating the task.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for update_task()</td></tr>
<tr><td> <em>Response:</em> <div>This code defines a function called update_task() which takes three parameters: an index<br>(int), a name (str), a description (str), and a due date (str).</div><div><br></div><div>The function<br>first checks if the specified index is within the valid range of indices<br>for the tasks list. If the index is invalid, an error message is<br>printed and the function returns.</div><div><br></div><div>If the index is valid, the function retrieves the<br>task at that index from the tasks list.</div><div><br></div><div>The function then checks if any<br>of the input parameters (name, description, due) are None. If any of them<br>are None, it sets the corresponding value to the current value of the<br>property for that task.</div><div><br></div><div>Next, the function checks if any of the input parameters<br>are different from the corresponding properties of the task. If any of them<br>are different, it updates the task with the new values provided and prints<br>a message indicating that the task was updated. If none of the values<br>are different, the function prints a message indicating that no changes were made<br>to the task.</div><div><br></div><div>The function then updates the lastActivity property of the task to<br>the current date and time using the datetime.now() function. Finally, the save() function<br>is called to save the updated tasks list to a file</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 4: </em> Mark Task Done/Complete Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited mark_done() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220066703-b099856e-9f20-444d-970d-4997293b8f55.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p> this code checks the index out of bonds and finds the task<br>from list by index. if the task is already marked done, it displays<br>a print statement saying that the task is already marked as done. if<br>the task is not marked as done, then this code updates the status<br>to done and prints the statement as &quot;test marked as done&quot;<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of mark_done()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220066875-b0b9a780-2362-441c-9581-e6fb027ae989.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>The program prompts the user to select the task they want to mark<br>as completed by entering its number from the &quot;list&quot; command.  The user<br>selects task number 1 and marks it as done. The program confirms that<br>the task was successfully marked as done.  However, the user then selects<br>the &quot;done&quot; command again and selects the same task number 1. The program<br>responds that the task is already marked as done, indicating that the user<br>had previously marked the task as done and is attempting to mark it<br>as done again.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for mark_done()</td></tr>
<tr><td> <em>Response:</em> <div>The function first tries to retrieve the task at the specified index from<br>the tasks list. If the index is invalid (i.e., outside the range of<br>valid indices for tasks), an error message is printed and the function returns.</div><div><br></div><div>If<br>the index is valid, the function checks if the done property of the<br>task is already True. If it is, a message is printed indicating that<br>the task is already marked as done and the function returns.</div><div><br></div><div>If the task<br>is not already marked as done, the function sets the done property of<br>the task to True and sets the completed_at property to the current date<br>and time using the datetime.now() function. A message is then printed indicating that<br>the task has been marked as done.</div><div><br></div><div>The save() function is then called to<br>save the updated tasks list to a file.</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 5: </em> View Task Logic (and list) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited view_task() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220067039-1cebccf4-8511-44c1-82a2-174b7d1d2759.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>this code checks whether there are any index out of bonds and finds<br>the task from the list using index. the code uses the given print<br>statement to display task name,description,last activity,due date and completion status<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of view_task()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220067116-91727fb3-c72d-4c47-bfdd-a1a7eeccc7de.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>user has selected the &quot;view&quot; command. The program prompts the user to select<br>the task they want to view by entering its number from the &quot;list&quot;<br>command.  The user selects task number 2, and the program displays the<br>information for the task. The task is named &quot;fitness&quot; and has a description<br>of &quot;workouts in the gym&quot;. The last activity associated with the task occurred<br>on February 17th, 2023 at 07:27:12.719148, and the task is due on February<br>24th, 2023 at 11:00:00. The task has not yet been completed, as indicated<br>by the &quot;Completed: -&quot; line at the end of the output. later the<br>user enters another input as the index of the list and the code<br>prints out that the task is not found as the index is out<br>of bonds.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Add screenshot(s) of list_tasks() output showing a few examples</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220067220-7ececec7-4bef-42fc-b12e-07827964fc16.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>user has selected the &quot;view&quot; command. The program prompts the user to select<br>the task they want to view by entering its number from the &quot;list&quot;<br>command.  The user selects task number 1.So, the details of the task<br>sports is shown whith a descriptioni &quot;playing badminton&quot; along with the last activity,<br>due date and completion status. In the same way the tasks are shown<br>for index 2 and index 3.At last, the user has given input as<br>4 which is out of the bonds and print statement displays the same<br></p>
</td></tr>
</table></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 6: </em> Delete Task Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited delete_task() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220067356-0e145414-6f0a-4a41-90d6-2f50abcbd4c7.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>this code check whether there are any index out of bonds and deletes<br>task from the list by using the index and saves the program<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of delete_task()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220069986-1f9c175a-4fb6-4df1-8d0a-adca84270e6e.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>The used selects the list and the tasks are dispalyed.Then the user then<br>selects the &quot;delete&quot; command and is prompted to enter the number of the<br>task they want to delete from the &quot;list&quot; command. The user selects task<br>number 2, and the program confirms that it has been successfully deleted. <br>However, the user then selects the &quot;delete&quot; command again and selects task number<br>4, which does not exist in the list. The program responds with an<br>error message indicating that index 3 is out of bounds, implying that the<br>user has entered an invalid task number.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for delete_task()</td></tr>
<tr><td> <em>Response:</em> <div>This code defines a function called delete_task() which takes an index parameter as<br>input.</div><div><br></div><div>The function first checks if the provided index is valid (i.e., within the<br>range of valid indices for tasks). If it is not, an error message<br>is printed and the function returns.</div><div><br></div><div>If the index is valid, the function uses<br>the del keyword to remove the task at the specified index from the<br>tasks list.</div><div><br></div><div>A message is then printed indicating that the task at the specified<br>index has been successfully deleted.If any other index which is out of bonds<br>is given, the code prints that the index is not found</div><div><br></div><div>The save() function<br>is then called to save the updated tasks list to a file.</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 7: </em> Get Incomplete Tasks Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited get_incomplete_tasks() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220067810-af536ac6-6ddd-4f6e-b83e-a6ba6da2ea77.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>this code takes all the tasks that not marked as done and appends<br>them to the list incomplete_tasks and  prints all the tasks that are<br>not done by calling the function list_tasks()<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of get_incomplete_tasks()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220067893-54ee8c77-8e9f-4dd6-962d-b1b05130754c.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>the user selects the option to view the task 1. the code shows<br>the task name, description and other details along with the completion status as<br>True. once again the user views the second task i,e chess which is<br>marked as &quot;-&quot; i,e incomplete. Till now, we have seen that there are<br>2 tasks i,e drawing (completed ) and chess(incomplete)  now, the user gives<br>the input as &quot;incomplete&quot;. The code shows the 2nd task i,e chess as<br>incomplete task.  the user then marks the task chess as &quot;done&quot; and<br>again checks the incomplete tasks. The code displays that &quot;there are no tasks<br>to show&quot;.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for get_incomplete_tasks()</td></tr>
<tr><td> <em>Response:</em> <div>This code defines a function called get_incomplete_tasks().</div><div><br></div><div>The function first creates an empty list<br>called _tasks.&nbsp;</div><div>The function then creates an empty list called incomplete_tasks.</div><div><br></div><div>The function then iterates<br>over each task in the tasks list. If the current task is not<br>marked as done, it is added to the incomplete_tasks list.</div><div><br></div><div>After all tasks have<br>been checked, the list_tasks() function is called with The incomplete task as input</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 8: </em> Get Over Due Tasks Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited get_overdue_tasks() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220068061-cb1bf68a-b048-40a8-af33-bbe9cccefad7.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>this code creates an empty list overdue_tasks and check whether there are any<br>due tasks which are older than now by comparing the due date and<br>present datetime. if yes, it calls the function list_tasks() and print all those<br>overdue tasks <br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of get_overdue_tasks()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220068287-2d270a19-57e5-43f2-be9e-1fd2235b39a5.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>The user first used the &quot;list&quot; command to view their task list, which<br>displayed three tasks - &quot;drawing,&quot; &quot;chess,&quot; and &quot;football.&quot;  They then used the<br>&quot;overdue&quot; command to view any tasks that are past their due date. In<br>this case, there was only one task, &quot;football,&quot; which was due on February<br>15th and had not yet been completed.  After that, the user used<br>the &quot;done&quot; command to mark the &quot;football&quot; task as completed, as it had<br>already passed its due date. The program then confirmed that the task was<br>marked as done.  Finally, the user used the &quot;overdue&quot; command again to<br>check if any tasks were still overdue, but the program returned that there<br>were no tasks to show since all tasks were either completed or not<br>yet due.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for get_overdue_tasks()</td></tr>
<tr><td> <em>Response:</em> <div>This code defines a function called get_overdue_tasks().</div><div><br></div><div>The function first creates an empty list<br>called _tasks.&nbsp;</div><div><br></div><div>The function then creates an empty list called overdue_tasks.</div><div><br></div><div>The function then gets<br>the current datetime using datetime.now() and stores it in the now variable.</div><div><br></div><div>The function<br>then iterates over each task in the tasks list. For each task, it<br>checks whether it is not marked as done and whether the due date<br>is older than the current datetime. If both conditions are true, the task<br>is added to the overdue_tasks list.</div><div><br></div><div>After all tasks have been checked, the list_tasks()<br>function is called with the overdue_tasks list as input.&nbsp;</div><div>if all the tasks are<br>marked as done and if the input is again given as "overdue", then<br>the code prints that there are no tasks to show</div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 9: </em> Get Time Remaining Logic </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of the edited get_time_remaining() function</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220068453-d3407c27-219c-412f-aff1-7ea2cfaecc6b.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>this code checks whether the index is out of bonds and gets the<br>task by index.    later, it calculates the difference between the<br>due date and current datetime    if the task is overdue,<br>it calculates the overdue datetime and converts the duration to days, hours, minutes,<br>seconds and displays the same<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) showing the success/failure of get_time_remaining()</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220068726-b75aa3f9-54be-46ff-81ae-a98c5eb62a17.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>the user first uses the &quot;list&quot; command to view a list of their<br>current tasks. The output shows three tasks: &quot;drawing,&quot; &quot;chess,&quot; and &quot;football.&quot; They then<br>use the &quot;remaining&quot; command to check how much time is remaining until the<br>due date for task number 1, &quot;drawing.&quot; The output shows that there are<br>7 days, 22 hours, 48 minutes, and 12 seconds left until the due<br>date.  After that, they use the &quot;remaining&quot; command again to check how<br>much time is overdue for task number 3, &quot;football&quot;, which is already overdue.The<br>output shows that the task is overdue by 1 day, 22 hours, 42<br>minutes, and 34 seconds.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Briefly explain the solutions to the checklist items for get_time_remaining()</td></tr>
<tr><td> <em>Response:</em> <div>get_time_remaining that calculates and displays the time remaining or overdue for a task<br>at a specified index in the tasks list.&nbsp;</div><div><br></div><div>The function accepts an index as<br>an argument and initializes an empty dictionary named task.</div><div>The function checks if the<br>index is valid, i.e., it should be non-negative and less than the length<br>of the tasks list. If it's not a valid index, the function prints<br>an error message and returns.</div><div>The function retrieves the task information by getting the<br>dictionary at the specified index in the tasks list and extracting the due<br>date of the task.</div><div>The function then converts the due date from a string<br>to a datetime object using the str_to_datetime function</div><div>The function calculates the time difference<br>between the current datetime and the due date of the task using the<br>datetime.now() function and the - operator.</div><div>If the time difference is negative, it means<br>the task is overdue, so the function flips the sign of the time<br>difference to get the overdue time.</div><div>The function then calculates the number of days,<br>hours, minutes, and seconds in the time difference using the .days and .seconds<br>attributes of the timedelta object.</div><div>Finally, the function prints the remaining time or overdue<br>time format by using an f-string. The string displays the number of days,<br>hours, minutes, and seconds in the time difference</div><div><br></div><div><br></div><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 10: </em> Misc </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707834-bf5a5b13-ec36-4597-9741-aa830c195be2.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Add screenshot(s) of program output generated from filling in this deliverable (or close to it)</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220108504-f509c6d3-0d1e-4a46-a69a-e4cb7788dcfe.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>the code asks the command and the input is given as add.Later the<br>task name is given as assignment with description complete assignment.the user enters the<br>due date and the task is added succesfully .Later, if no inputs are<br>given to the command add, it displays &quot;task not added sucessfully&quot;.the user gives<br>the input as list.the code displays the list of the tasks.the user later<br>deletes the task at index 2 succesfully.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220117221-b209f880-f0a5-43ab-b121-53d7c25b0b55.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>in the above screenshot, MP1-Tracker was created and python file tracker is attached<br>to it and later status of the file is checked and file is<br>commited<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Add screenshot(s) of the saved JSON file</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123112989/220127436-affdb20f-8006-42e5-bea9-ee5282ddfc9e.PNG"/></td></tr>
<tr><td> <em>Caption:</em> <p>the json file has 3 tasks regarding sports, fitness, music. For all these<br>3 tasks . descriptions were given along with the last activity , due<br>date, completion status .<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 3: </em> Discuss any issues and how they were overcome or learnings from this project</td></tr>
<tr><td> <em>Response:</em> <p>From this project , I have leant to code a task manager which<br>can add tasks and update it whenever needed. One can view their tasks<br>to know whether they are completed or not.this task manager also helps the<br>user to delete the task and know about the incomple tasks and their<br>time duration.I have learnt the usage of functions , dictionaries , lists, json<br>module, date time module, handling errors through try and except .<br></p><br></td></tr>
<tr><td> <em>Sub-Task 4: </em> Add pull request for this assignment (project branch to dev)</td></tr>
<tr><td>Not provided</td></tr>
</table></td></tr>
<table><tr><td><em>Grading Link: </em><a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IS601-004-S23/is601-mini-project-1-tracker-app/grade/ar2565" target="_blank">Grading</a></td></tr></table>